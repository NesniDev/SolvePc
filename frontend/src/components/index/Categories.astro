---
import { categories } from '@mockup/categories'
---

<section>
  <div class="wrapper-title">
    <h2>02 / Categorías</h2>
    <h3>Explora toda la base de diagnósticos.</h3>
  </div>

  <div class="categories">
    {
      categories.map(({ name, icon, articles }) => (
        <div class="category">
          <div>
            <img src="/categories/arrow-link.svg" alt="Icono de Hardware" />
          </div>
          <img src={icon} alt="Icono de Hardware" />
          <h3>{name}</h3>
          <p>{articles} Articulos</p>
        </div>
      ))
    }
  </div>
</section>

<style>
  .wrapper-title {
    display: flex;
    flex-direction: column;
    justify-content: space-evenly;
    align-items: start;
    gap: 10px;
    width: 100%;
    h2 {
      font-size: 15px;
      letter-spacing: 2px;
      margin-top: 40px;
      margin-bottom: 0;
      color: var(--color-hard);
      font-family: 'Bangers', system-ui;
    }
    h3 {
      font-size: 30px;
      margin-top: 10px;
      margin-bottom: 10px;
      color: #eef7fb;
      text-transform: uppercase;
      font-family: 'Grandstander Variable', system-ui;
    }
  }
  section {
    max-width: 1000px;
    margin: 0 auto;
    display: flex;
    flex-wrap: nowrap;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
    margin-top: 50px;
  }

  .categories {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
    width: 100%;
    font-family: 'bangers', system-ui;
  }

  .category {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: start;
    gap: 10px;
    width: 115px;
    background-color: #0e1c2e;
    border-radius: 15px;
    padding: 20px;
    position: relative;
    transition: all 0.3s ease-in-out;
    will-change: transform;
    border: 1px solid transparent;
    border: 1px solid rgba(0, 255, 255, 0.05);
    &:hover {
      background-color: #0e1c2e;
      transform: scale(1.02);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.548);
      border-radius: 15px;
      border: 1px solid rgba(0, 255, 255);
    }
    & div > img {
      position: absolute;
      transform: rotate(-45deg);
      top: 10px;
      right: 10px;
      width: 25px;
      height: 25px;
      opacity: 0;
      transition: all 0.3s ease-in-out;
      cursor: pointer;
      &:hover {
        transform: rotate(-90deg);
      }
    }
    &:hover div > img {
      opacity: 1;
    }
    & img {
      width: 25px;
      filter: drop-shadow(0 0 10px rgba(0, 255, 255, 0.548));
    }
    & h3 {
      font-size: 20px;
      font-weight: 700;
      letter-spacing: 2px;
      color: #eef7fb;
      margin: 0;
    }
    & p {
      font-size: 10px;
      text-transform: lowercase;
      color: #949494;
      font-family: 'JetBrains Mono Variable', monospace;
      margin: 0;
    }
  }
</style>
