services:
  db:
    container_name: db_solvepc
    image: postgres:14.23
    volumes:
      - solvepc-db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: solvepc
      POSTGRES_DB: solvepc
    ports:
      - 5439:5432
volumes:
  solvepc-db-data: